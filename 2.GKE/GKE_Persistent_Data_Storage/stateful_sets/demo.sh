useful for stateful applications.

statefulsets run and maintain set of pods just like deployments do.

statefulset object defines a desired state and its controller achieves it.
However unlike deployments statefulsets maintain a persistent identity for each pod.

